# Channel Adapters

Channel adapters connect Fugue to external messaging services and protocols.

Each adapter implements a common interface for sending and receiving messages. Built-in adapter types include:

- **IRC** -- Internet Relay Chat connections.
- **Discord** -- Discord bot and user integrations.
- **Matrix** -- Matrix protocol support.
- **Stdin/Stdout** -- Local console adapter for testing and scripting.
