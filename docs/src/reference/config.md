# Config Reference

This page documents every supported key in the Fugue TOML configuration file.

The configuration is organized into sections: `[general]`, `[adapters]`, `[vault]`, and `[plugins]`. Each section controls a different aspect of Fugue's behavior.

Refer to [Configuration](../configuration.md) for a higher-level overview of how configuration works.
