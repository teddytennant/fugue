# Configuration

Fugue uses a TOML configuration file to control its behavior.

By default, the configuration file is located at `~/.config/fugue/config.toml`. You can specify an alternate path with the `--config` flag.

The configuration file defines channel adapters, plugin settings, and vault options. See the [Config Reference](reference/config.md) for a full list of supported keys.
